# 参数敏感性分析参考要点

本文档整理仓库中 `参数敏感性分析参考.pdf` 与参数敏感性分析相关的关键结论。

## 参考文献

- Hu, S., Shi, L., Zha, Y., Williams, M., & Lin, L. (2017). Simultaneous state-parameter estimation supports the evaluation of data assimilation performance and measurement design for soil-water-atmosphere-plant system. Journal of Hydrology, 555, 812-831.

## 敏感性分析的总体范围

- 作者对 56 个 SWAP/WOFOST 参数进行了 Morris 敏感性分析。
- 这些参数不是单纯作物参数, 而是土壤-作物耦合系统参数。
- 从数量上看, 作物生长相关参数占绝大多数, 土壤水力参数只占少数。
- 因此更准确的表述是: 对土壤-作物模型中的 56 个参数进行整体敏感性分析, 其中参数集以作物生长相关参数为主, 同时包含关键土壤水力参数。

## 敏感性分析输出变量

作者评估 56 个参数对以下模型输出的敏感性:

- `LAI`: 叶面积指数.
- `SSM`: 表层土壤水分.
- `DVS`: 发育阶段.
- `WS`: 水分胁迫.
- `grain yield`: 最终籽粒产量.
- `aboveground biomass`: 地上部生物量, 用于本 MAIZSIM 算例补充作物生长响应.

本仓库第一轮 LOAM2D 实现中, 这些输出需要映射为可从 MAIZSIM 文件中稳定抽取的标量指标:

| 参考输出 | LOAM2D 第一轮指标 | 文件和字段 | 聚合方式 |
|---|---|---|---|
| `LAI` | `max_lai` | `LOAM2D.g01` 的 `LAI` | 作物生长期内最大值 |
| `SSM` | `mean_top20_theta` | `LOAM2D.G03` 的 `thNew`, `Y <= 20 cm` 节点 | 按 `Area` 加权得到每日 0-20 cm 平均含水量, 再对作物生长期取时间均值 |
| `WS` | `seasonal_transpiration_deficit` | `LOAM2D.G05` 的 `SeasPTran`, `SeasATran` | 最后一日计算 `1 - SeasATran / SeasPTran`, 当 `SeasPTran <= 0` 时记为缺失 |
| `aboveground biomass` | `final_shoot_dm` | `LOAM2D.g01` 的 `shootDM` | `Note` 首次为 `Matured` 的记录; 若无 `Matured`, 使用最后一条记录并标记未成熟 |
| `grain yield` | `final_ear_dm` | `LOAM2D.g01` 的 `earDM` | `Note` 首次为 `Matured` 的记录; 若无 `Matured`, 使用最后一条记录并标记未成熟 |

`DVS` 在当前 LOAM2D 输出文件中没有直接字段。第一轮实现不把 `DVS` 或物候事件替代量作为敏感性输出指标; 如后续需要分析发育阶段响应, 应先增加或确认可靠的发育阶段输出字段。

除上述可对应 Hu 文献输出的主指标外, `SingleLayerLoam2D` 算例增加 `final_shoot_dm` 作为地上部生物量指标, 并增加 `seasonal_potential_transpiration` 和 `seasonal_actual_transpiration` 作为作物蒸腾指标。两个蒸腾指标分别直接取 `LOAM2D.G05` 最后一日的 `SeasPTran` 和 `SeasATran`, 用于区分潜在蒸腾需求、实际蒸腾实现量和相对蒸腾亏缺。

## 方法设置

- 使用 Morris 方法筛选敏感参数.
- 参数扰动范围为默认值的 +/-10%.
- 参数分布假定为正态分布.
- Hu 文献中的 Morris 轨迹数为 `r = 500`.
- 敏感性排序使用改进 Morris 指标 `mu*`, 即 elementary effects 绝对值均值.
- 本仓库实现使用 `SALib.sample.morris.sample` 和 `SALib.analyze.morris.analyze`; 默认 `num_levels = 4`, `seed = 20260429`.
- 本仓库当前实现保留两级运行: smoke `r = 2`, formal `r = 150`. smoke 用于快速验证采样、回写、并行调度和输出解析; formal 是正式敏感性分析规模, 对 13 个参数对应 2100 个模型运行。Hu 文献中的 `r = 500` 只作为文献参考和未来可选扩展, 当前不作为运行目标。

## Hu 方法的实现理解

Hu 的敏感性分析不应理解为传统固定基线的单因素扰动。传统 OAT 做法是在其它参数保持默认值时, 分别改变某一个参数, 再比较输出变化。Hu 使用的是 Morris 全局筛选方法: 每个 elementary effect 的计算确实只沿一个参数方向步进, 但这个步进发生在多维参数空间中的不同采样点上, 其它参数并不长期固定在基线值。

因此, 若按 Hu 思路实现本仓库的敏感性分析, 应采用 Morris 轨迹采样:

- 在参数允许范围内生成多条 Morris 轨迹.
- 每条轨迹由若干个采样点组成, 相邻采样点只改变一个参数.
- 对同一参数, 收集其在不同背景参数组合下的 elementary effects.
- 用 `mu*` 作为主要敏感性排序指标.
- 可同时记录 elementary effects 的标准差, 用于判断非线性和参数交互影响.
- 实现时必须采用并行批量运行结构, 即同时启动多个样本运行; 不应按样本完全串行执行, 否则 Morris 所需的大量模型运行会导致总耗时过长. 本仓库当前推荐 smoke 和 formal 运行时显式设置 `--max-workers 16`.

可以额外保留少量 OAT 运行作为调试或方向性检查, 但正式敏感性排序不应只用固定基线下的高低扰动结果替代 Morris。

## 56 个参数的组成

### 土壤水力参数 5 个

1. `thetaR`: 残余含水量.
2. `thetaS`: 饱和含水量.
3. `Alfa`: van Genuchten 主干燥曲线 alpha 形状参数.
4. `npar`: van Genuchten 形状参数 n.
5. `Ksat`: 饱和垂向导水率.

### 作物参数 51 个

1. `TSUMEA`: 出苗到抽雄/开花的积温.
2. `TSUMAM`: 开花到成熟的积温.
3. `TDWI`: 初始作物总干重.
4. `LAIEM`: 出苗期叶面积指数.
5. `RGRLAI`: LAI 最大相对增长率.
6. `SPAN`: 35 deg C 下叶片寿命.
7. `TBASE`: 叶片衰老下限温度.
8. `SLATB0`: DVS = 0 时比叶面积.
9. `SLATB0.78`: DVS = 0.78 时比叶面积.
10. `SLATB2.0`: DVS = 2.0 时比叶面积.
11. `EFF`: 真实叶片光能利用效率.
12. `AMAXTB0`: DVS = 0 时最大 CO2 同化速率.
13. `AMAXTB1.25`: DVS = 1.25 时最大 CO2 同化速率.
14. `AMAXTB1.5`: DVS = 1.5 时最大 CO2 同化速率.
15. `AMAXTB1.75`: DVS = 1.75 时最大 CO2 同化速率.
16. `AMAXTB2.0`: DVS = 2.0 时最大 CO2 同化速率.
17. `TMPFTB9`: 平均温度 9 deg C 时 AMAX 折减因子.
18. `TMPFTB16`: 平均温度 16 deg C 时 AMAX 折减因子.
19. `TMPFTB18`: 平均温度 18 deg C 时 AMAX 折减因子.
20. `TMPFTB20`: 平均温度 20 deg C 时 AMAX 折减因子.
21. `CVL`: 同化物转化为叶片的效率.
22. `CVO`: 同化物转化为贮藏器官的效率.
23. `CVR`: 同化物转化为根的效率.
24. `CVS`: 同化物转化为茎的效率.
25. `Q10`: 温度每升高 10 deg C 呼吸速率相对增加系数.
26. `RML`: 叶片维持呼吸速率.
27. `RMO`: 贮藏器官维持呼吸速率.
28. `RMR`: 根维持呼吸速率.
29. `RMS`: 茎维持呼吸速率.
30. `RFSETB1.75`: DVS = 1.75 时衰老折减因子.
31. `RFSETB2.0`: DVS = 2.0 时衰老折减因子.
32. `FRTB0`: DVS = 0 时分配到根的干物质比例.
33. `FRTB0.4`: DVS = 0.4 时分配到根的干物质比例.
34. `FRTB0.6`: DVS = 0.6 时分配到根的干物质比例.
35. `FRTB0.9`: DVS = 0.9 时分配到根的干物质比例.
36. `FLTB0`: DVS = 0 时地上部干物质分配到叶的比例.
37. `FLTB0.33`: DVS = 0.33 时地上部干物质分配到叶的比例.
38. `FLTB0.88`: DVS = 0.88 时地上部干物质分配到叶的比例.
39. `FSTB0`: DVS = 0 时地上部干物质分配到茎的比例.
40. `FSTB0.33`: DVS = 0.33 时地上部干物质分配到茎的比例.
41. `FSTB0.88`: DVS = 0.88 时地上部干物质分配到茎的比例.
42. `FOTB1.05`: DVS = 1.05 时地上部干物质分配到贮藏器官的比例.
43. `FOTB2.0`: DVS = 2.0 时地上部干物质分配到贮藏器官的比例.
44. `RDRRTB1.5001`: DVS = 1.5001 时根相对死亡率.
45. `RDRRTB2.0`: DVS = 2.0 时根相对死亡率.
46. `RDRSTB1.5001`: DVS = 1.5001 时茎相对死亡率.
47. `RDRSTB2.0`: DVS = 2.0 时茎相对死亡率.
48. `COFAB`: 截留系数.
49. `RDI`: 初始根深.
50. `RRI`: 最大日根深增长速率.
51. `RDC`: 作物/品种最大根深.

## 土壤参数在后续实验中的使用

敏感性分析阶段的候选土壤参数为 5 个: `thetaR`, `thetaS`, `Alfa`, `npar`, `Ksat`.

后续同化实验中, 作者重点选入的不确定土壤参数主要是每层 3 个:

- `ThetaS`.
- `npar`.
- `Ksat`.

单层土壤 Cases 1-7 中对应为:

- `ThetaS1`.
- `npar1`.
- `Ksat1`.

分层土壤 Cases 8-10 中, 每层都有 3 个土壤参数:

- 上层: `ThetaS1`, `npar1`, `Ksat1`.
- 下层: `ThetaS2`, `npar2`, `Ksat2`.

## Baseline 模型输入是否完整

作者给出了敏感性分析 baseline 的关键配置, 但没有详细到仅凭论文即可完整复现输入文件。

已明确说明的信息包括:

- 模型为 SWAP, 作物为 maize.
- 气象数据来自 Wageningen weather station.
- 模拟时间为 1981-05-01 到 1981-10-15, 共 168 天.
- 气象变量包括太阳辐射、最低/最高气温、相对湿度、风速和降水, 时间尺度为日尺度.
- 上边界为 atmospheric boundary condition.
- 下边界为 free drainage.
- 潜在蒸散由 Penman-Monteith 方法计算.
- 敏感性分析所用土壤为 loam.
- 最大根深为 100 cm.
- 出苗初始根深为 10 cm.
- 潜在日根深增长速率为 1.5 cm/d.
- 56 个参数默认值来自文献和 SWAP/WOFOST 文档.

未完整给出的信息包括:

- SWAP/WOFOST 完整输入文件.
- 每日气象数据表.
- 初始土壤含水量或压力水头剖面.
- 播种、出苗、管理措施等完整作物管理输入.
- 土壤剖面离散、网格和完整数值设置.
- Morris 方法中 `p` 水平数的具体取值.
- 可直接运行的敏感性分析代码.

因此, 论文提供的是方法和关键配置层面的 baseline, 不是完整可直接复现的 baseline 输入数据集。

## 土壤属性是否为单层

敏感性分析阶段可以理解为单层均质土壤, 土壤类型为 loam。

判断依据:

- 附录 A 明确写明 `The soil is loam.`
- 56 个敏感性分析参数中的土壤参数只有一组, 没有区分 layer 1 和 layer 2.
- 后续 Cases 1-7 明确使用 one layer of loam soil with 200 cm depth.
- 分层土壤是后续 Cases 8-10 才额外引入的情景, 上层 0-30 cm 为 loam, 下层 30-200 cm 为 silt loam.

可采用的表述:

> 作者的 56 参数 Morris 敏感性分析对应的是单层 loam 土壤 baseline; 分层土壤不是敏感性分析 baseline, 而是后续同化实验中为了测试复杂土壤剖面额外设置的情景。
